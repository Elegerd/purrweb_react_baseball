export const getViewedProfile = (state) => state.viewedProfile.data;

export const getViewedProfileError = (state) => state.viewedProfile.error;

export const getViewedProfileIsLoading = (state) => state.viewedProfile.loading;
