export const getBattingGraph = (state) => state.battingGraph.data;

export const getBattingGraphError = (state) => state.battingGraph.error;

export const getBattingGraphIsLoading = (state) => state.battingGraph.loading;
