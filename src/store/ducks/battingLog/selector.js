export const getBattingLog = (state) => state.battingLog.data;

export const getBattingLogError = (state) => state.battingLog.error;

export const getBattingLogIsLoading = (state) => state.battingLog.loading;
